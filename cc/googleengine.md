make new folder and add files
main.py
app.yaml

now write code as per requirements in main.py and app.yaml file

now open google app engine and 
file > add existing project > select the folder where main.py and app.yaml files are located > open

then click on run then wait for it to run after that browse the app in browser or open http://localhost:8080/
